Task 5: main branch updated before merging feature-max.
Task 6: main branch updated before rebasing feature-min.



Task answers



1\. Git does a fast-forward merge when the main branch has no new commits after the new branch was created. A merge commit is created when both branches have new commits.



2\. Merge keeps the branch history. Rebase moves commits on top of another branch and makes the history look more linear and clean.



3\. In my repository, the conflict was solved manually in Program.cs. I removed the conflict markers and kept one final message that combines both changes.

